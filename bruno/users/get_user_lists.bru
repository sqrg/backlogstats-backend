meta {
  name: Get User Lists
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/v1/users/{{userId}}/lists
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  limit: 100
  offset: 0
}
