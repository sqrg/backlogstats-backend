meta {
  name: Get User Collection
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/v1/users/{{userId}}/collection
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  limit: 100
  offset: 0
}
