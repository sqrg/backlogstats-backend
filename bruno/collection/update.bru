meta {
  name: Update Collection Entry
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/api/v1/collection/{{collectionId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "platform_id": 2
  }
}
