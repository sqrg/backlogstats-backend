meta {
  name: Add to Collection
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/collection
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "game_id": {{gameId}},
    "platform_id": {{platformId}}
  }
}
